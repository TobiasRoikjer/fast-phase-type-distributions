#include "mat.h"

int mat_mult(mat_t *left, mat_t *right) {
    return 0;

    /*
    if right col key > left row key
        inc right
    else if <
        inc left
    else
        sum += left * right*/
}